# Module aisdk

The specification: `LanguageModel` and its modality siblings, `Content`, `StreamPart`,
`CallOptions`, `Prompt`, errors. Serializable where a consumer must persist and replay a turn;
in-memory otherwise. Knows nothing about any host app.

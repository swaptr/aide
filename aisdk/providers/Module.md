# Module providers

The vendors. Native wires (Anthropic, Google, OpenAI Responses, Cohere, Bedrock, Vertex, and the
media vendors) plus the OpenAI-compatible table (`Vendors`), where a vendor is configuration —
a base URL, dialect enums, capability flags — never a subclass.

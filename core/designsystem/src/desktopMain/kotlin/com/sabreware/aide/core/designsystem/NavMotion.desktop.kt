package com.sabreware.aide.core.designsystem

/** Pointer: no gesture to mirror and no predictive back to scrub — a short fade through, not travel. */
internal actual val navStyle: NavStyle = NavStyle.Fade

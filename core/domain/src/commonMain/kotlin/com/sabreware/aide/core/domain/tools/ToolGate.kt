package com.sabreware.aide.core.domain.tools

// User-toggleable tool gates (formerly AideToolRegistry.Gated).
// Filesystem is gated by ToolCategory.FILESYSTEM instead (single source of truth).
enum class ToolGate { WEB_SEARCH }
